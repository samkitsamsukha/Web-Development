const names: string[] = [];

names.push("Samkit");

console.log(names[0]);

const marks: number[] = [];
marks.push(90);
marks.push(92);
marks.push(94);

const names2: Array<string> = [];

type User = {
    name: string
    isActive: boolean
}

const db: User[] = [];
db.push({name:'Samkit', isActive: true});
console.log(db[0]);

const MLmodel: Number[][] = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];
console.log(MLmodel);